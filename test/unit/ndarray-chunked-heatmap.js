import { cheatmap } from '../../src/ndarray-chunked-heatmap';

describe('cheatmap', () => {
  let chm;

  beforeEach(() => {
    chm = cheatmap();
  });

  it('should do something', () => {
  });
});
